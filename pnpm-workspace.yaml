# Approve the native dependencies whose install scripts must run to build their
# binaries. pnpm 11 blocks build scripts by default; `allowBuilds: <pkg>: true`
# is the canonical approval (it supersedes the older onlyBuiltDependencies list).
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
